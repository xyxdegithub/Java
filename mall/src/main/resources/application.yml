server:
  port: 8888

mybatis:
  mapper-locations: classpath:mappers/*.xml
  type-aliases-package: com.xyx.entity


spring:
  datasource:
    druid:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://localhost:3306/mall?useUnicode=true&serverTimezone=UTC&characterEncoding=utf-8&useSSL=false
      username: root
      password: root